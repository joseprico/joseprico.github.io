# Històric temporada 2025-26 — CN Terrassa Waterpolo Stats

Còpia **congelada** de l'app tal com va acabar la temporada 2025-26 (equips Cadet i Juvenil).
No editar. Es manté com a arxiu històric.

L'app nova es desenvolupa a `../app-nova/`.

## Contingut

Snapshot dels fitxers de l'app viva en el moment de tancar la temporada:

- `index.html` — l'aplicació sencera (vanilla, un sol fitxer).
- `service-worker.js`, `manifest.json` — PWA.
- `OneSignalSDKWorker.js`, `OneSignalSDKUpdaterWorker.js` — push.
- Icones (`*.png`).

## Notes

- Segueix llegint les dades històriques dels repos de GitHub Pages
  (`cnt_cadet_25-26`, `CNT_juvenil_25_26`) i del mateix Firebase (`cnt-wp-stats-bb7dc`).
  Mentre aquestes fonts existeixin, l'snapshot continua funcionant per consulta.
- Data de congelació: final temporada 2025-26.
